package com.krya1012.clearance

import android.app.Application

/**
 * App-scoped singletons (Room database, DataStore) land here once the
 * data layer is introduced in a later milestone.
 */
class ClearanceApplication : Application()
