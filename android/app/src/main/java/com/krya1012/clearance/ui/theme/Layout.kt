package com.krya1012.clearance.ui.theme

import androidx.compose.ui.unit.dp

/** Layout constants mirrored from iOS `Theme.swift`'s `Layout` enum. */
object Layout {
    val ScreenPadding = 20.dp
    val CardCornerRadius = 18.dp
    val RowCornerRadius = 14.dp
    val CheckboxSize = 28.dp
    val SectionSpacing = 22.dp
}
